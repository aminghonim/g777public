// Backward-compatibility shim. All logic moved to wa_status_provider.dart.
export 'wa_status_provider.dart';

// Legacy aliases for existing widget code
// cloudStatusProvider → waStatusProvider (via export)
// CloudStatus → WAStatus (via export)
// CloudStatusNotifier → WAStatusNotifier (via export)
